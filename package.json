{
  "name": "lcars",
  "version": "1.0.0",
  "description": "this is a simple app to display a pixelart version of the original Star trek UI aka LCARS",
  "scripts": {
    "build": "tsc && node node_modules/@next-flip/fz-sdk-mntm/sdk.js build",
    "start": "npm run build && node node_modules/@next-flip/fz-sdk-mntm/sdk.js upload"
  },
  "devDependencies": {
    "@next-flip/fz-sdk-mntm": "^0.1",
    "typescript": "^5.6.3"
  }
}